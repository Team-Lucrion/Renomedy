version: "3.9"

services:

  api:
    build: ./backend
    container_name: renomedy-api
    ports:
      - "8000:8000"

  ollama:
    image: ollama/ollama
    container_name: renomedy-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data: