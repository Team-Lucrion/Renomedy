Use the latest Swasthi backend audit as source of truth. Do not re-audit from scratch. Execute the remaining launch-blocking operational work only.

Current backend readiness: 91/100.
Goal: move to controlled closed-beta operational readiness.

STRICT PRIORITY ORDER:

1. APPLY WEBHOOK AUDIT MIGRATION
Apply:
supabase/migrations/20260507130500_webhook_audit_hardening.sql

Then verify:
- migration exists in target DB
- audit_logs can store webhook failures even when no mapped user exists
- /admin/issues surfaces webhook failures correctly

2. VERIFY LIVE CLERK WEBHOOK
Do not only inspect code. Run live verification.

Endpoint:
POST /auth/clerk-webhook

Verify:
- user.created creates exactly one user row
- user.deleted removes/deactivates mapped user correctly
- invalid Svix signature returns 400
- duplicate/replayed event does not create duplicate user
- failed webhook is written to audit_logs
- founder can see failure in /admin/issues

Output exact evidence:
- command used
- response status
- DB row/result checked
- any failure

3. VERIFY FIREBASE FCM LIVE PUSH
If push notifications are in beta scope, test real delivery.

Verify:
- Firebase Admin credentials load correctly
- device token registration works
- POST /notifications/test-push works
- reminder push path works
- refill push path works
- invalid token cleanup works
- push failures are logged/auditable

Output exact evidence:
- command used
- token registration result
- delivery result
- failure logs if any

4. CLARIFY OCR BETA STANCE
OCR_PROVIDER=mock is acceptable only if beta is positioned as assisted/manual verification.

Do NOT integrate real OCR unless explicitly required.
Instead output:
- current OCR_PROVIDER
- whether scan flow is demo-grade
- manual verification fallback
- exact beta wording to avoid overclaiming

5. FINAL RUNTIME READINESS REPORT
Output only:
- migrations applied
- webhook verification result
- FCM verification result
- OCR stance
- remaining blockers
- launch-blocking vs non-blocking
- updated beta readiness score

RULES:
- No new features
- No founder UI
- No growth loops
- No ABHA
- No redesign
- No broad architecture audit
- Do not stay in planning mode
- Execute and verify live where credentials/environment allow