# OCR Migration Validation Checklist\n\n- [x] Integrate ML Kit Document Scanner in Frontend\n- [x] Send extractedText to backend on scan\n- [x] Update scan-prescription backend endpoint to accept extractedText\n- [x] Implement MlKitMedGemmaProvider OCR provider\n- [x] Fallback correctly handled on the edge vs server-side\n- [x] Add MedGemma schemas and validation (already present in base codebase but correctly utilized now)\n\n# Rollback Strategy\n\nIf the ML Kit or MedGemma pipeline encounters production issues, roll back the backend configuration to the old stable provider (e.g. `VISION_GEMINI`) by changing the `OCR_PROVIDER` environment variable without requiring a client-side update. Since the frontend simply falls back to the backend default if OCR fails locally or isn't required by the backend, the backend is the authoritative source for the final OCR method.
